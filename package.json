{
  "name": "gitlab-few",
  "version": "0.0.1",
  "main": "src/lib/index.coffee",
  "dependencies": {
    "lodash": "^3.8.0",
    "moment": "^2.10.3",
    "q": "^1.4.0",
    "request": "^2.55.0"
  },
  "scripts": {
    "pretest": "coffee --no-header -o ./ -c ./src/",
    "test": "coffee src/test/project-test.coffee",
    "prepublish": "coffee --no-header -o ./ -c ./src/"
  }
}
