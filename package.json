{
  "name": "gitlab-few",
  "version": "0.0.6",
  "main": "lib/index.js",
  "dependencies": {
    "lodash": "^3.8.0",
    "moment": "^2.10.3",
    "q": "^1.4.0",
    "request": "^2.55.0"
  },
  "scripts": {
    "test": "coffee --no-header -o ./ -c ./src/",
    "prepublish": "coffee --no-header -o ./ -c ./src/"
  },
  "devDependencies": {
    "should": "^7.0.1"
  }
}
